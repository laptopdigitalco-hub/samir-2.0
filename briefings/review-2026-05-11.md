## Code Review — samir-2.0 — 2026-05-11

**Score: 7/10** — Solid foundation, one meaningful disconnect to fix before launch.

### Strengths
- All six blueprints (CBH-001, MB-001, PMS-001, LFU-001, PROP-001, COB-001) have clear triggers, failure handling, and explicit permission gates
- Security hygiene is good — all credentials use environment variables, no hardcoded secrets
- Three Engine Model is documented and consistently applied
- Voice rules enforced across blueprints and external comms

### Issues

| Severity | File | Finding | Fix |
|----------|------|---------|-----|
| High | `equipment/generate_proposal_pdf.py` | Najim Travel's data is hardcoded in the PROPOSAL dict. PROP-001 says read from pipeline sheet — script ignores this | Refactor to accept client data as CLI args or config input |
| High | `intel/focus.md`, `intel/wins.md` | 12 days stale. Quarterly goals undefined. Launch is 4 days away | Update both files now |
| Medium | `equipment/generate_proposal_pdf.py` | `os.environ["KEY"]` raises silent KeyError on missing env vars | Use `.get()` with a clear exit message |
| Medium | `equipment/code_review_agent.py` | Imports `python-dotenv` but makes no API calls — unused dependency | Remove the import |
| Medium | `.claude/skills/` vs `.claude/commands/` | morning-briefing and pipeline-summary duplicated across both dirs | Pick one as source of truth |
| Medium | `intel/stack.md` | Gmail listed as no live credentials — Gmail MCP confirmed working | Update to reflect actual status |
| Low | `CLAUDE.md` | /review and /audit commands missing from file map | Add and mark as built |

### Top 3 to action this week
1. Fix `generate_proposal_pdf.py` — hardcoded client data will generate the wrong proposal for every future client
2. Update `intel/focus.md` and `intel/wins.md` before 15 May launch
3. Remove unused `dotenv` import from `code_review_agent.py`

*Review generated by Claude Code — 2026-05-11*
